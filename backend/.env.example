# Database
MONGODB_URI=mongodb://127.0.0.1:27017/bookvalley

# Auth
JWT_SECRET=75ff7bd25aa1d389
JWT_EXPIRES_IN=7d

# How to generate a secure JWT secret:
# Option 1: Use Node.js (run in terminal):
#   node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
# Option 2: Use a password generator
#   https://passwordsgenerator.net/ (choose at least 32 characters)

# Stripe
STRIPE_SECRET=https://api.stripe.com/v1/charges

# Frontend URLs (comma-separated)
FRONTEND_URL=https://book-valley.vercel.app,http://localhost:5173

# Server
PORT=5000
NODE_ENV=production
